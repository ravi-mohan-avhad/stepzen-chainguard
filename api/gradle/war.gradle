apply plugin: "war"

bootWar {
    mainClass = "com.chainguard.sz.api.ChainguardApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
